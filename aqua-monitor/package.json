{
  "name": "aqua-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Aqua Monitor - Feeding / DeadFish / Transfer / Harvest tek uygulama",
  "scripts": {
    "start": "fiori run --open index.html",
    "build": "ui5 build --clean-dest",
    "deploy": "npm run build && fiori deploy --config ui5-deploy.yaml"
  },
  "devDependencies": {
    "@ui5/cli": "^4.0.0",
    "@sap/ux-ui5-tooling": "^1.15.0"
  }
}
